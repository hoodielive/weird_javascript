function words() {
	console.log('there sits a compiler in javascript - your code (lexically) -> compiler -> computer instructions')
}; 

words(); 

# Execution Context: a wrapper to help manage the code that is running! 
	# Very important! 
