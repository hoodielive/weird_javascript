// for loops

for (var x = 1; x < 5; x++) {
    console.log('Know your limitations and transcend them!');
    console.log(x);
}

// iterate through arrays

var names = ["Frodo", "Sam", "Merry", "Pippin"];

for (var i=0; i < names.length; i++) {
    console.log(names[i]); 
}
