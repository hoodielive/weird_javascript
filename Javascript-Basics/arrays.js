var points = [25, 16, 7, 9, 31];

// add items to an array - push
points.push(8);

// remove elements - pop
var last = points.pop();
console.log(points, last);

// indexing
var first = points[0];
console.log(first);

// find the length
console.log(points.length); 

console.log(points);
